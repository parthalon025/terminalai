# TerminalAI Requirements
# =======================
# Install: pip install -r requirements.txt
# Or full install: pip install -e .

# Core dependencies
yt-dlp>=2023.0.0          # YouTube downloading
pyyaml>=6.0               # Configuration files

# GUI dependencies
gradio>=4.0.0             # Modern web interface

# Note: External dependencies (install separately)
# - FFmpeg with NVENC support
# - NVIDIA Driver 535+
# - NVIDIA Maxine Video Effects SDK (optional, for AI upscaling)
#   Download from: https://developer.nvidia.com/maxine

# For development/testing, use: pip install -e ".[dev]"
# This installs: pytest, pytest-cov, black, ruff
